import customtkinter as customtk

class MainApp(customtk.CTk):
    def __init__(self):
        super().__init__()
        self.geometry("800x600")

        self.button = customtk.CTkButton(self, text="Create Category")
        self.button.pack(padx=20, pady=20)
    
app = MainApp()
app.mainloop()

